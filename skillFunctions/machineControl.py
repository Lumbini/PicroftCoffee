def controlFunction(keyword):
	if (keyword == "off"):
		# send message to turn off coffee (LED)
		# Might be useful to first check if the coffee machine is already off,
		#	or in the middle of brewing. 
	elif (keyword == "on"):
		# Send message to turn on coffee

